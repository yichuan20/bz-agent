# Production env — frontend is served from the same Python server
# Empty string = relative URLs = same origin (no hardcoded localhost)
VITE_AGENT_HTTP_URL=
# WebSocket uses the same host but a different path/port
# Leave blank to use the runtime-detection in main.tsx (see below)
VITE_AGENT_WS_URL=
